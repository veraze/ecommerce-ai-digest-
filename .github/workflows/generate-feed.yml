name: Generate Feeds

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Feeds are generated locally via residential IP and pushed directly. See fetch-tweets-local.sh."
